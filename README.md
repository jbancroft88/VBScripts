# VBScripts

**PDFAutoMailer**
*A macro which will automatically export each sheet of an Excel workbook to a PDF file and attach it to an email message with the recipient SMTP address. An example use case would be to automate the distribution of billing information to multiple indiviudals from one workbook.*
